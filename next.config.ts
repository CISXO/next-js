import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    reactStrictMode: false,
    // output: "export", //$ npx serve@latest out -> pnpm run build 후에 실행
    // output: "standalone"
};

export default nextConfig;
